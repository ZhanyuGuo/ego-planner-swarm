

int main(int argc, char const *argv[])
{
    // a simple way to valid sfm-model
    // disable ego_planner
    // enable sfm_planner
    // publish quadrotor_msgs::PositionCommand with update x, y, z to /position_cmd
    // centerlized control first: all position known, all controllable...
    return 0;
}
